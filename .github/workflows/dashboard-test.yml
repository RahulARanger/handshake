name: 'dashboard-test'

on:
  push:
    branches:
      - 'dashboard-build'

permissions:
    contents: write

jobs:
    test:
      name: Test Dashboard
      runs-on: 'ubuntu-latest'
      permissions: write-all

      steps:
        - name: Checkout code
          uses: actions/checkout@v3

        - name: Use Node.js
          uses: actions/setup-node@v3
          with:
            node-version: 20.9.0

        - name: Setup Turbo Repo
          run: cd graspit-reporters && npm install && npm run build

        - name: Install all and Run Tests
          working-directory: ./graspit-reporters/apps/test-mocha
          run: npm install && npm run test:mocha

