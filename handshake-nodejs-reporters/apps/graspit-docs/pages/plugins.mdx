import SetupSteps from "pages/components/setupSteps";

<>
	<SetupSteps current={1} />
</>

## Getting Started

In this step, we would setup Custom Reporter in your test Automation framework
so we can send the test results to the graspit server whenever required.
In this step, we will not be writing the custom reporter but we will just use the existing ones
