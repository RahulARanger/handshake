import SetupSteps from "pages/components/setupSteps";

<>
	<SetupSteps current={1} />
</>

## Getting Started

By Plugins, we could either mean Custom Reporter or custom config for hooks. Test Automation Framework would have hooks in place `beforeTest`, `afterTest`, `onTestPass`, etc...,
We would utilize this hooks to communicate with the server.
