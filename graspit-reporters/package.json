{
  "name": "graspit-reporters",
  "packageManager": "npm@9.6.2",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "sanity": "turbo run sanity --concurrency=1 --continue",
    "test": "npm run build && turbo run test --concurrency=1 --continue",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "wdio": "wdio run ./apps\\test-mocha\\wdio.conf.js",
    "publish": "turbo run build lint && changeset version && changeset publish",
    "generate-report": "graspit export --out ./dist ../TestResults",
    "view-report": "graspit display ./dist"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.13.1",
    "@typescript-eslint/parser": "^6.13.1",
    "eslint": "^8.54.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-config-airbnb-typescript": "^17.1.0",
    "prettier": "^3.1.0",
    "rimraf": "^5.0.5",
    "tsconfig": "7.0.0",
    "turbo": "^1.10.16",
    "tsup": "^8.0.1",
    "@changesets/cli": "^2.27.1"
  },
  "dependencies": {
    "log4js": "^6.9.1"
  }
}
